[project]
name = "personal-task-mcp-server"
version = "2.0.0"
description = "Authenticated MCP server for personal task management"
requires-python = ">=3.12"
dependencies = [
    "mcp>=2.1,<3",
    "python-dotenv>=1.1,<2",
]
