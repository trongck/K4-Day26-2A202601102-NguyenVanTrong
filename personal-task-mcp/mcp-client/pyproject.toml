[project]
name = "personal-task-mcp-client"
version = "1.0.0"
description = "Google ADK web agent for the Personal Task MCP Server"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.3,<3",
    "mcp>=1.28,<2",
    "python-dotenv>=1.1,<2",
]
